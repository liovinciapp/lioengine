package lioengine

func (b *Bot) analizeUpdates() (updates []*Update) {
    return
}

// layer wraps all layer functions that will be executed on 
// *Bot.analizeUpdates()
type layer struct {}

var layers  = []*layer{}